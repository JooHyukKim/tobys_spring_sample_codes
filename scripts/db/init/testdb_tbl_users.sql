CREATE DATABASE testdb;

CREATE TABLE testdb.users(
                          id VARCHAR(100) PRIMARY KEY,
                          name VARCHAR (100),
                          password VARCHAR (100)
);
